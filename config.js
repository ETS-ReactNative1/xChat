export default config = {
    apiurl: "https://quickmeet.societyplus.net",    //API Location WITH trailing /
    retryTime1: 2,                                   //Time to first retry request if failed (seconds).
    retryTime2: 4,                                   //Time to second retry request if failed (seconds).
    retryTime3: 10,                                   //Time to first retry request if failed (seconds).
}